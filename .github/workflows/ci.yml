name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - run: pip install -r requirements.txt pytest
      - run: python -m py_compile scan.py learn.py generate.py upload.py app/*.py app/scanner/*.py spike/*.py scripts/*.py
      - run: pytest -q
